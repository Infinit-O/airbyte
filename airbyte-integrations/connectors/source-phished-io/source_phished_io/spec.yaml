documentationUrl: https://docsurl.com
connectionSpecification:
  $schema: http://json-schema.org/draft-07/schema#
  title: Phished Io Spec
  type: object
  required:
    - organization_id 
    - secret
    - integration_id
  properties:
    # 'TODO: This schema defines the configuration required for the source. This usually involves metadata such as database and/or authentication information.':
    organization_id:
      type: string
      description: Taken from the PowerBI integration URL.
    secret:
      type: string
      description: Look at the PowerBI Integration docs for instructions.
    integration_id:
      type: string
      description: Generate an "integration URL" to get these.
