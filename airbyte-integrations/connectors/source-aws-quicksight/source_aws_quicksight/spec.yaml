documentationUrl: https://docsurl.com
connectionSpecification:
  $schema: http://json-schema.org/draft-07/schema#
  title: Quicksight Spec
  type: object
  required:
    - aws_account_id
    - aws_access_key_id
    - aws_secret_access_key
    - aws_region_name
  properties:
    aws_account_id:
      type: string
      description: describe me
    aws_access_key_id:
      type: string
      description: describe me
    aws_secret_access_key:
      type: string
      description: the AWS secret access key for the acount
    aws_region_name:
      type: string
      description: The AWS region identifier, e.g 'ap-east-1'
