{
  "documentationUrl": "https://docsurl.com",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Zscaler Spec",
    "type": "object",
    "required": ["api_key", "username", "password", "base_url"],
    "additionalProperties": false,
    "properties": {
      "api_key": {
        "type": "string",
        "description": "The API key assigned to you by the admin."
      },
      "username": {
        "type": "string",
        "description": "The username (usually email) of the service account."
      },
      "password": {
        "type": "string",
        "description": "The password to the service account."
      },
      "base_url": {
        "type": "string",
        "description": "The base URL assigned to your organization, e.g. https://zscaler.zsapi.net/"
      }
    }
  }
}
